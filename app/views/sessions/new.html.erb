<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login - UBBCSA</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.3/css/bulma.min.css">
  <link rel="stylesheet" href="login.scss">
</head>
<body>
  <section class="section">
    <div class="container">
      <div class="columns is-centered">
        <div class="column is-half">
          <h1 class="title has-text-centered">Bienvenido a UBBCSA</h1>
          <h2 class="subtitle has-text-centered">Iniciar sesión</h2>

          <!-- Flash messages for errors and notifications -->
          <% flash.each do |key, message| %>
            <div class="notification is-<%= key == 'alert' ? 'danger' : 'primary' %>">
              <button class="delete"></button>
              <%= message %>
            </div>
          <% end %>

          <div class="box">
            <%= form_with url: login_path, local: true do |form| %>
              <div class="field">
                <label class="label">Email</label>
                <div class="control">
                  <%= form.email_field :email, class: "input", placeholder: "Email" %>
                </div>
              </div>
              <div class="field">
                <label class="label">Contraseña</label>
                <div class="control">
                  <%= form.password_field :password, class: "input", placeholder: "Contraseña" %>
                </div>
              </div>
              <div class="field is-grouped is-grouped-centered">
                <div class="control">
                  <%= form.submit "LOGIN", class: "button is-dark" %>
                </div>
                <div class="control">
                  <%= link_to "Iniciar sesión con Google", "#", class: "button google" %>
                </div>
                <div class="control">
                  <%= link_to "REGISTRARSE", signup_path, class: "button signup" %>
                </div>
              </div>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </section>
  <script>
    document.addEventListener('DOMContentLoaded', () => {
      (document.querySelectorAll('.notification .delete') || []).forEach(($delete) => {
        const $notification = $delete.parentNode;
        $delete.addEventListener('click', () => {
          $notification.parentNode.removeChild($notification);
        });
      });
    });
  </script>
</body>
</html>
